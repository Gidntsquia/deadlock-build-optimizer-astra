# Revision plan — fixed before revised held-out evaluation

The user requested a reference-driven shop/timeline redesign and improved top-player agreement.

Design uses the installed WSL frontend-design skill and the supplied item-shop and ability-order screenshots. Palette: charcoal #171d1d, parchment #e9dfc7, shop board #8b8068, cyan #78bac6, spirit #b780e4, weapon gold #caa660. Georgia headings and Trebuchet/system body text. Item cards, not rows, are the dominant element; category-colored name strips, Roman tier corners, and purchase numbers provide shop context. Four parallel ability tracks show unlock and 1/2/5 AP operations. Desktop shows the whole track; phone splits it into two consecutive eight-step tracks. Full written order stays available.

Generator revision is a single predeclared aggregate-only change set, not iterative fitting to Zergggy's exposed sample. Preserve the 30-match sample, the 30% core rule, the 70/30 evaluator, and all scoring weights. Fetch Ascendant-and-above aggregate data (min_average_badge=100) over the existing seven complete UTC days, using it where >=1000 hero matches and >=30 item rows exist. Retain the all-skill snapshot as a fallback. Correct candidate investment bonus to use upgrade payment after owned-component credit. Replace rigid per-phase category quotas with two focus slots, one vitality slot, and two unrestricted slots; preserve three phases, fifteen purchases, inventory legality, and distinct approaches. Require empirical usage evidence (>=20 matches) when enough legal observed candidates exist; a non-observed asset is fallback only. This targets population mismatch and forced unobserved recommendations independently of held-out item choices.

Evaluate once after these choices and source are fixed. Report any increase or decrease honestly. Do not change the evaluation denominator, discard inconvenient matches, or use the result to choose further weights/items. Existing agreement is 31% spirit, 34% weapon. These are re-used test results, not fresh independent generalization evidence.
